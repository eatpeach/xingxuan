# 发票打印页样式对齐报价单

| 项目 | 内容 |
|---|---|
| **状态** | ✅ 完成（2026-08-11，待生产肉眼复验） |
| **负责人** | 开发人员A |
| **指派人** | 老板（2026-08-11 验收 07 时提出） |
| **创建时间** | 2026-08-11 |
| **时限** | 不急（不阻塞验收） |
| **风险等级** | 🟢 低（纯前端样式，不碰数据/逻辑） |

## 背景

`6a1dfb0`「改用斑兔企服版式」重构了两个打印页，但**各写了一套 CSS**：
- 报价单 `QuotePrint.tsx` → `q-*` 类名体系（q-head/q-meta/q-table/q-sum/q-grand/q-foot…）
- 发票 `InvoicePrint.tsx` → `i-*` 类名体系

之后 `163a092` 又只改了报价单（去「此致」落款），两页差距进一步拉大。

老板 08-11 验收 07 号单时反馈：**发票的字体、样式有问题，参考报价单调整。**

## 怎么做

**以报价单（`q-*`）为基准，把发票的字体/字号/间距/对齐向它看齐。** 不必强行合并成一套类名
（那样改动面大、风险高），但视觉规格要一致：

- [ ] 字体族、字号档位（标题/表头/正文/辅助）对齐 q-*
- [ ] logo 与公司名间距、抬头区排布对齐
- [ ] 表格：列对齐规则（数字右对齐 tabular-nums、居中列）、行高、边框对齐 q-table
- [ ] 合计区（i-totals / i-grand）视觉对齐 q-sum / q-grand
- [ ] 页脚三栏（i-foot）对齐 q-foot
- [ ] 三语切换下都不撑破（印尼文最长）

## 🔴 正确姿势（踩过的坑，别盲改）

本机无登录态、CSS 内联在 tsx 的模板串里。**必须**：
1. 把 `InvoicePrint.tsx` 和 `QuotePrint.tsx` 的 style 串抽成 css 文件 + 静态 mock html
2. headless Chrome 截图对比，在 scratchpad 里迭代到两页视觉一致
3. 再回写 tsx，`npm run build` 提交 dist

**不要直接改 tsx 里的 CSS 值靠想象**——报价单/发票的排版问题历史上都是这么被截图暴露的。

## 验证

- [x] 本地样张：scratchpad mock 并排截图（headless Chrome），字体/间距/表格/页脚一致
- [ ] 生产（需登录）：打开一张真实报价单 + 一张真实发票，肉眼确认成套 ← 待老板验

## 完成记录（2026-08-11）

把发票 `i-*` 的字号/间距/分隔线/表格/合计/页脚/灰底块全部对齐报价单 `q-*`：

- **表格**（核心）：由「蓝底白字表头 + 斑马纹 + 仅横线」→「浅灰 `#f5f5f5` 表头 + 全边框 `1px #333`」，与 `q-table` 同一套；加 `table-layout:fixed` + `word-break/overflow-wrap`（三语防撑破）
- 公司名 18→22px/700→800；分隔线 3→2px；表头 12→14px
- 合计 grand 17→15px；付款信息块 `#f7f9fc`→`#fafafa`+边框（对齐 `q-notes`）
- 页脚 logo 22→36px、去居中改左对齐、字号→13px（对齐 `q-foot`）
- 发票特有的日期/落款/付款信息/条款**保留**，仅对齐视觉规格
- 未合并类名（按单要求，改动面小）；`InvoicePrint.tsx` 一处文件，`npm run build` 通过，dist `index-H3W3Isj2.js`
