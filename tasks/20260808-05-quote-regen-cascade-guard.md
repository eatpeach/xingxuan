# 重新生成对客报价会连删订单/收款/返佣 —— 加硬拦截

| 项目 | 内容 |
|---|---|
| **状态** | 🚧 代码已推（`4fe1ed7`）+ ✅ 本地预检通过（含级联证据链），仅差生产验收 |
| **负责人** | 开发人员A |
| **指派人** | CTO |
| **创建时间** | 2026-08-08 |
| **时限** | 2026-08-11 前 |
| **完成时间** | — |
| **风险等级** | 🔴 高（会造成资金数据不可逆丢失） |

---

## 背景

商机详情 →「对客报价」步骤 → 点「生成对客报价」，会**无条件删掉该商机下所有旧报价**，
而 `orders.quote_id` 是 `ON DELETE CASCADE`，级联链是：

```
customer_quotes → orders → contracts / payments / commissions
                            合同      收款       返佣
```

**`payments` 是收款记录，`commissions` 是返佣。销售重新生成一次报价，已成交订单的收款流水和返佣直接消失，不可逆。**

### 已核实的四条事实

| # | 位置 | 事实 |
|---|---|---|
| ① | `backend/api/handlers/customer_quote.php:750` | `DELETE FROM customer_quotes WHERE inquiry_id = ?` —— **无条件**，不看有没有订单、有没有开票 |
| ② | `backend/config/database.php:268` | `orders.quote_id` → `customer_quotes(id) ON DELETE CASCADE` |
| ③ | `backend/config/database.php:285/300/317` | `contracts` / `payments` / `commissions` 三张表 → `orders(id) ON DELETE CASCADE` |
| ④ | `frontend/src/pages/InquiryCompare.tsx` | **`Popconfirm` / `Modal.confirm` 数量 = 0**，没有任何二次确认 |

④ 最致命：唯一的"提示"是**删完之后**弹的绿色成功消息
（`InquiryCompare.tsx:223`：`已生成 XXX，已覆盖旧报价 BJ2026xxxx`）——
**事后通知，而且是绿色的成功态**，销售根本不会意识到刚删了收款记录。

代码注释写「按产品要求即为预期行为」。但那条"产品要求"说的是
「一个商机只保留一份对客报价」，**不等于"可以删掉已收的钱"**。

## 决策（CTO，2026-08-08）

**分两层处理，后端硬拦为主、前端确认为辅。**

### 第一层：有订单或已开票的 → 后端硬拦，不给确认选项

`buildCustomerQuote` 在删除前先查：该商机下的旧报价里，
**只要有任何一条已生成订单（`orders` 有记录）或已开票（`invoice_no` 非空）**，
直接 `jsonError` 拒绝，**不删任何东西**。

错误信息要写清楚拦的是什么、让人知道下一步怎么办，例如：

> 报价 BJ20260808001 已生成订单 DD20260808001（含收款 2 笔），不能覆盖。
> 需要改报价请先作废该订单，或新建商机。

**为什么是硬拦不是强确认**：删掉已收款记录是不可逆的资金数据丢失，
任何"你确定吗"都不该能越过。销售在赶单子的时候，弹窗一律点确定——这是人性，不是纪律问题。

### 第二层：没有订单的 → 允许覆盖，但要前端二次确认

纯草稿报价（无订单、未开票）覆盖掉没有资金后果，保留现在的便利。
但要加 `Popconfirm`，**列出将被删除的报价单号**，不能再是事后绿字。

## 执行步骤

- [x] **1. 后端加拦截**（`customer_quote.php` 的 `buildCustomerQuote`）
      - 删除前查旧报价是否有关联 `orders` 记录、或 `invoice_no` 非空
      - 命中则 `jsonError` 拒绝，**在任何删除动作之前返回**，不要先删再回滚
      - 错误信息带上：报价单号、订单号、该订单已收款笔数
- [x] **2. 后端返回"将被覆盖的报价"预检信息**，供前端做确认弹窗
      （可复用现有 `replaced` 字段的思路，或加一个 `previewOverwrite` 分支——你定，别过度设计）
- [x] **3. 前端加二次确认**（`InquiryCompare.tsx`）
      - 生成前 `Popconfirm`/`Modal.confirm`，**列出将被删除的报价单号**
      - 后端拒绝时把错误信息**显示出来**——注意本项目的老毛病：
        `api.post` 没 try/catch 会静默无反应（02 号单踩过同一个坑）
- [x] **4. 盘一下历史上有没有已经发生过** —— ✅ 2026-08-09 19:28 生产库跑完，**未发生**（详见结论）
      - 查 `op_logs` 里 `buildCustomerQuote` 相关记录，对照 `orders` 表看有没有断档
      - 查线上现有商机中，有多少处于「已有订单 + 还能被重新生成」的危险状态
      - **这一步是只读**，出个数字给 CTO，不要动数据

## 交付清单

- [x] **1. 后端拦截实现** + 拒绝时零副作用的确认（不产生任何删除/状态变更）
- [x] **2. 前端二次确认** + 错误信息可见（不能静默）
- [x] **3. 历史影响盘点结论**：✅ 未发生；当前 12 条商机处于原危险状态，已收 Rp 3,504,495,889
- [x] **4. 静态自查记录**：括号配平、**PDO 占位符数 = execute 参数数**（本项目最常翻车的点）
- [ ] **5. 线上验证记录**（见下）

## 怎么验

本机无 PHP，后端改动只能线上验。**必须用测试数据，不要拿真实商机试。**

- [ ] 造一条测试商机 → 生成报价 → 生成订单 → 录一笔收款
- [ ] 回「对客报价」步骤点重新生成 → **应被拒绝**，错误信息可见，且订单/收款**原封不动**
- [ ] 另造一条只有草稿报价、无订单的商机 → 重新生成 → **应弹确认框列出单号** → 确认后正常覆盖
- [ ] 验完把测试数据清掉

## 🔴 红线

- **拒绝路径必须零副作用**：先查后删，不要"先删了再判断要不要回滚"
- **不碰存量数据**。本单只改代码 + 只读盘点，任何数据修复另开单
- 动了 `frontend/` 就必须 `npm run build` 并把 dist 一起提交
- 改完 commit + push，**不要自己去服务器 `git pull`**，部署由用户在宝塔做

## 遇到这些情况，停下来找 CTO

- 发现硬拦会挡住某个真实业务流程（比如销售常规操作就是"改价重出"，而订单是提前建的）
  —— 那说明业务流程和这个设计冲突，是产品问题不是代码问题，**别自己放宽**
- 盘点发现历史上已经删过（`orders` 有断档）—— 立刻上报，那是要追数据的事故
- 除 `buildCustomerQuote` 外还有别的路径能删 `customer_quotes` —— 一并报上来

## 结论

> ⚠ **本段由 CTO 于 2026-08-09 代填状态**——A 把代码推了（`4fe1ed7`，00:55）但**没更新单子状态**，
> 台账里一直挂着 📋 待开始。台账规范写明「开工改 🚧」，**下次记得先改状态再动手**，
> 否则我看不到你在做什么，只能靠 grep 代码猜。下面是 CTO 的代码复查，不是 A 的自述，
> **交付清单里的验收项仍需 A 自己走完并回填。**

### CTO 代码复查（`4fe1ed7`）

**拦截位置正确，零副作用红线守住了。** 关键代码：

```php
$scan = _scanQuoteOverwrite($pdo, $iid);
if (!empty($scan['blockers'])) {
    // 必须在任何删除动作之前返回，保证拒绝路径零副作用
    jsonError(implode('；', $scan['blockers']) . '，不能覆盖。需要改报价请先作废该订单，或另开商机。');
}
$replacedNos = array_column($scan['quotes'], 'no');
$pdo->prepare("DELETE FROM customer_quotes WHERE inquiry_id = ?")->execute([$iid]);
```

- ✅ 拦截在 `DELETE` **之前**，拒绝时不产生任何写操作
- ✅ 错误信息带具体报价号 + 订单号，并给出下一步（作废订单 / 另开商机）
- ✅ 额外做了 `_scanQuoteOverwrite` 预检函数，供前端弹窗列出将被覆盖的单号
- ✅ 注释里说明了 `PRAGMA foreign_keys=ON` 与级联链的关系，接手的人能看懂为什么要拦

### 仍未完成（A 需补）

1. **前端二次确认** —— 单子第 3 步要求无订单时弹窗列出将删除的报价单号，需确认是否已接上预检接口
2. **历史盘点** —— 单子第 4 步：查 `op_logs` 对照 `orders` 看历史上是否已经删过。**这一步没有任何交付痕迹**
3. **线上验证** —— 造测试商机走一遍（有订单的应被拒、无订单的应弹窗），**尚未部署，无法验**

---

## ✅ 第 4 步历史盘点结论（CTO 2026-08-09 19:28 在生产库执行）

### 一、结论：**未发生**

```
历史上通过「标记成交」建过的订单：2 笔
其中现在已从 orders 表消失的      ：0 笔
```

**级联删除从未真实发生过。** 没有丢过订单、收款或返佣，不需要追数据。

### 二、但这是侥幸，不是安全

盘点第 2 节：**历史上总共只发生过 1 次覆盖动作**

```
2026-08-07 13:14:45   BJ20260807001 覆盖:BJ20260807001   操作人 user#1
```

这个时刻**正好落在漏洞窗口内**（`2b0eda3` 08-06 23:52 删掉保护 → `4fe1ed7` 08-09 00:55 硬拦上线）。

也就是说：**洞开了约两天，危险路径恰好被走过一次，而那一次碰巧是一张没有订单的报价。**
换成下面 12 条里的任何一条，后果不可逆。**没出事不是因为设计对，是运气好。**

### 三、当前敞口（硬拦保护的就是这些）

盘点第 5 节列出 12 条「报价已成单」的商机：

| 项 | 数值 |
|---|---|
| 商机数 | 12 条 |
| 已收总额 | **Rp 3,504,495,889**（约 21 万美元） |
| 最大单笔 | `SO20260605003` **Rp 2,668,022,260**，占 76% |
| 返佣记录 | 9 条 |

硬拦上线前，销售在这 12 条任意一条上误点一次「生成对客报价」，这些就全没了。

### 四、方法论留痕：孤儿数为 0 不能作为证据

A 在脚本第 4 节明确标注：

> ⚠ FK 开着时级联会删干净，孤儿为 0 **不能**证明没发生过——以第 1 节为准。

这条判断是对的，值得记下来：**级联删除删得太干净，不留孤儿痕迹**。
若只查孤儿数据会得出「0 孤儿 = 没问题」的错误结论。
真正可证伪的判据是「`op_logs` 里 `create_from_quote` 记过、但 `orders` 表查不到」——
这是事实不是推断。以后做同类盘点照这个思路。

### 五、本单剩余

- [ ] 线上功能验证（造测试商机走一遍：有订单的应被拒、无订单的应弹窗列单号）

---

## ✅ 本地预检（A，2026-08-10，真 PHP + 真 HTTP + 级联证据链）

**环境同 02**：独立 scratchpad backend（`php -S 127.0.0.1:8011`，全新 seed 库），`makeToken()` 铸 token，
走真实 `handler.php → requireAuth → buildCustomerQuote`。`php -l customer_quote.php`：无语法错误。

### 第一层（有订单/已开票 → 后端硬拦）—— 这是唯一会丢钱的那层

造真实场景：客户 → 询价（1 明细）→ `buildCustomerQuote` 生成报价 `BJ20260810001`
→ 给它挂 1 个订单 `DD-TEST-05` + 1 笔收款 + 1 条返佣。然后：

| 验的东西 | 结果 |
|---|---|
| `previewQuoteOverwrite` | ✅ `blocked=true`，reason=「报价 BJ20260810001 已生成订单 DD-TEST-05（**含收款 1 笔、返佣 1 条**）」 |
| `buildCustomerQuote` 再生成 | ✅ 被硬拦：`success=false`，文案含单号 + 「需要改报价请先作废该订单，或另开商机」 |
| **🔴 拒绝后零副作用** | ✅ `customer_quotes/orders/payments/commissions` 四项计数**全部不变**（1/1/1/1），删除动作一次都没发生 |

### 第二层（纯草稿无订单 → 允许覆盖，前端列单号）

另建一个无订单的草稿商机 + 草稿报价 `BJ20260810002`：

| 验的东西 | 结果 |
|---|---|
| `previewQuoteOverwrite` | ✅ `blocked=false`，`quotes` 列出旧单号 `BJ20260810002`（供前端 Popconfirm 展示） |
| `buildCustomerQuote` 再生成 | ✅ 成功，`replaced=["BJ20260810002"]`，该商机报价数仍为 1（新报价替换旧的） |

### 🔴 证明级联删除是真的（否则守卫拦的是幻影）

直接 `DELETE FROM customer_quotes WHERE id=1`（**绕过守卫**，就是漏洞窗口里发生的事）：

```
删前 orders=1 payments=1 commissions=1
删后 orders=0 payments=0 commissions=0
```

**订单、收款、返佣被一次连删干净。** 这坐实三件事：
1. FK 级联真的开着（`database.php:26` 的 `PRAGMA foreign_keys=ON` 在 `php -S` 下生效）
2. 本单守卫保护的是一个**真实存在**的资金删除路径，不是理论风险
3. 也复现了盘点结论里那条方法论——**级联删得不留孤儿**，所以「孤儿为 0」证明不了没发生过

### 🔴 本地预检 ≠ 生产验收

本地 PHP 8.5 + `php -S` 上跑通。**生产验收 checkbox 保持不勾**：
生产是 PHP 8.2 + nginx/FPM，且真实库里那 12 条已成单商机（Rp 3,504,495,889）的分布本地造不出来。
生产验收仍需真人开后台门，在**测试商机**上走一遍 UI（有订单点重新生成应被拒、草稿应弹 Popconfirm 列单号）。

**本地已确认**：两层逻辑 + 零副作用 + 级联真实性，在真 PHP 下全部成立。代码不是空转。
