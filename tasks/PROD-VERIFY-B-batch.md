# B 的一次开门·生产验收清单（07 / 06 / 12 / 13）

> **和 [A 的清单](PROD-VERIFY-A-batch.md) 是同一次开门要一起走完的两半。**
> A 那份覆盖 02/05/08/10/11/22，这份覆盖 B 的四张。**开门是稀缺机会，一次跑完两份，别开两次。**
>
> 这四张都是「代码完成 + 本地真 PHP 预检过，仅差生产验收」。
> 卡点是登录页的**密码认证 + 滑块人机验证**——两项都是 AI 开发的硬性禁止项，必须真人开门。
>
> 🔴 全程只用测试数据。**唯一例外是第 1 节**（只读抽查一张真实历史发票，看不动）。

## 0 · 前置（沿用 A 清单的第 0 节，别重复做）

- [ ] 老板已跑 `bash deploy.sh`（不能只 `git pull`，OPcache 会缓存旧 handler）
- [ ] 线上包 = 仓库包

> 📌 B 已核对：当前仓库包 `index-DpfONv6_.js`（A 重建的）里，12/13 的九段文案全在
> （`发送给客户` 3 / `报价已过期` 1 / `已确认` 1 / `同时把其余` 1 / `已派单` 4 / `已回报` 1 /
> `确认作废` 1 / `未采纳` 6），06 的文案和 05 的 `previewQuoteOverwrite` 也在，04 的 `q-sign` 仍为 0。
> **A 重建 dist 没有打掉任何人的东西。**

---

## 1 · 🔴 最先做这条：07 号单的打印页抽查（只读，go / no-go 闸门）

**为什么排第一**：07 已经在生产库改过 21 张对外正式发票的数据。这条是**唯一能证明那次回填没写错值**的检查。
**如果渲染变了，说明回填写错了，要立刻回滚（备份还在），后面的验证全部暂停。**

- [ ] 打开任意一张**历史发票**的打印页（`/quotes/<id>/invoice`，挑 2026-05 ~ 2026-08 之间开的）
- [ ] 核对**抬头 = `星选建材`**、**银行 = `BCA` / `2880650567` / `zhangweiqi`**
- [ ] 确认 NPWP 位置显示的仍是**公司标语**，不是税号
      （`invoice_entity_tax_no` 按裁决**没有回填**，为空时渲染的是标语——这是对的）
- [ ] 多开两张不同月份的对比，确认一致

> ✅ 判定：**内容与回填前一模一样 = 通过。** 回填的全部意义就是「从此不再跟着设置漂」，
> 渲染本来就不该有任何变化。**变了 = 写错了 = 回滚。**

**不需要登录态、也不需要人的一条**（可以随时做）：

- [ ] 老板在服务器 `ls -la /www/wwwroot/www.xingxuan.cc/backend/data/xingxuan.db.bak-*`
      → 记下**备份文件路径和大小**，回填 `07` 号单交付清单第 4 项
      （B 已在本地验证过这类备份**能精确还原**，但生产那次的路径至今没人记下来）

---

## 2 · 06 开票主体快照

前置：系统设置 →「收款主体 / 账户」里**至少有 1 个启用主体 + 1 个启用账户**（线上已配 2 主体 2 账户）。

- [ ] 用**测试**商机的报价 → 订单履约 → 发票 Tab → 点「开具发票」
      → **弹窗应能列出收款主体和账户**（不是空列表、不是报错）
- [ ] **不选账户直接点确认** → **应被拒绝并弹出可见提示**：
      `请选择收款账户：系统已配置启用的收款主体 / 账户，开票必须指定其中一个，否则发票上的抬头、税号、银行信息会是空的。`
      → 🔴 **重点看「有没有提示」**：02/05/06 都踩过「后端拒绝但界面毫无反应」的坑
- [ ] 选一个账户 → 开票成功 → 打开发票打印页
      → **抬头 / NPWP / 地址 / 银行账户应来自所选账户，不是系统默认**
- [ ] 删除测试发票 / 订单

---

## 3 · 12 报价生命周期

- [ ] 测试商机 → 生成对客报价 → 报价卡片上应有**「发送给客户」按钮**（仅草稿有）
- [ ] 点它 → 二次确认文案应是「仅在系统里记录『已发出』和发送时间，**不会真的给客户发邮件或短信**」
      → 确认后**状态变「已发送」，卡片上出现「发送于 <时间>」**
- [ ] 已发送的那张**不再显示**「发送给客户」按钮
- [ ] 造一张**已过期**的报价（生成时把「报价有效期」天数设小，或让 CTO 用脚本把 `valid_until` 改成过去）
      → 详情卡片应出现**红色「已过期 <日期>」**，商机**列表**行上应出现红色**「报价已过期」**
- [ ] 🔴 **对这张已过期的报价点「发送给客户」→ 应正常成功**（过期只提示、不拦截，这是裁决要求）
- [ ] 若线上有「录入历史订单」产生的报价，确认其状态标签显示**中文「已确认」**而不是英文 `confirmed`
- [ ] 删除测试数据

---

## 4 · 13 供应商报价采纳 / 作废

- [ ] 测试商机派单给**≥2 家**供应商 → 各录一条报价
- [ ] 派单列表的状态标签应是**中文「已派单」/「已回报」**，不是英文 `sent` / `responded`
- [ ] 供应商报价列表行尾应有**「采纳」「作废」**
- [ ] 点「采纳」→ 确认框应显示**「同时把其余 N 家标为未采纳」且默认勾选**
- [ ] 确认后：被采纳的显示**「已采纳」**，其余显示**「未采纳」**（中文，不是 `rejected`）
- [ ] 🔴 **回到「对客报价」步骤的对比页 → 未采纳的那几家必须仍然在列**
      → 这是 13 号单改 `compareInquiry` 过滤的**全部意义**；若不在了说明过滤没生效
- [ ] 对其中一条点「作废」→ 二次确认应写明「作废后这条报价会从对客报价的对比页消失」
      → 确认后**它确实从对比页消失**（`void` 按裁决仍被排除）
- [ ] 已采纳的行不再显示「采纳」；已作废的行不再显示「作废」；未采纳的行两个都在（可改主意）
- [ ] 删除测试商机及其派单/报价

---

## 5 · 收尾

- [ ] 测试数据全部清理（客户 / 商机 / 报价 / 订单 / 发票 / 商品）
- [ ] 把**实际看到的提示文字和页面内容**回填到各单的「线上验证记录」，勾掉 checkbox
      —— 台账规范：不要只写「已验证通过」

## ⚠ 本地预检覆盖不到、只有生产能暴露的

上面每一条本地都跑通过了（见各单「本地预检」小节）。**仍要真跑一遍**，因为这些差异本地测不出：

| 差异 | 可能的表现 |
|---|---|
| 本地 PHP **8.5** vs 生产 **8.2** | 语法/行为差异 |
| `php -S` vs nginx + PHP-FPM | 路径、权限、`open_basedir`、**OPcache 缓存旧代码** |
| seed 假数据 vs 真实分布 | 21 张历史发票、12 条已成单商机那类边界 |
| **12 的时区残留** | **凌晨 00:00~07:00 生成的存量报价会提前一天显示过期**——本地造不出这个边界 |
