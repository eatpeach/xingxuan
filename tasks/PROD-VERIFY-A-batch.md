# A 的一次开门·生产验收清单（02 / 05 / 08 / 10 / 11 / 22）

> 用途：这六张单都是「代码完成 + 本地真 PHP 验证过，仅差生产验收」。
> 生产验收都需要后台登录态，而登录页有密码 + 滑块人机验证——**两项都是开发的硬性禁止项**。
> 所以攒成一份清单，**真人开一次门照着走完**，比每单单独约一次真人高效。
>
> 🔴 全程只用测试数据，验完清理。涉及资金/删单的步骤（05）尤其小心，别碰真实商机。
> 线上有 12 条已成单、Rp 3,504,495,889 收款 + 9 条返佣，碰错一条就是真事故。

## 0 · 开门前（不需要登录，先确认部署到位）

- [ ] 老板在宝塔跑 `bash deploy.sh`（**不能只 git pull**——OPcache 会缓存旧 handler.php，新 action 报「未知 action」）
- [ ] 确认线上包 = 仓库包：`curl -s https://www.xingxuan.cc/ | grep -oE 'assets/index-[A-Za-z0-9_-]+\.js'` 应等于 `frontend/dist/index.html` 里的入口名
- [ ] 首页 HTTP 200（顺带就是 10 号单的冒烟：seed 每请求都跑，改了 seed 没崩）

## 1 · 共享测试数据（建一次，02/05/11 复用）

- [ ] 建 1 个测试客户「_测试勿动」、1 个测试供应商「_测试勿动」

## 2 · 逐单验收

### 02 上架价格闸门
- [ ] 建 0 价测试商品 → 点「审核通过」→ **应弹「商品未定价（底价为 0），不能上架」，状态仍 pending**
- [ ] 后台商品列表对这条 0 价商品点「上架」→ **应同样被拒并弹出原因**（不是静默无反应）
- [ ] 补价 → 再审核通过 → **应上架成功**
- [ ] 打开 `https://www.xingxuan.cc` 货架 → 应能看到它；确认空库时是空态不是 500
- [ ] 删除测试商品

### 05 报价重生成级联拦截（最危险，只用测试数据）
- [ ] 用测试客户建测试商机 → 生成对客报价 → 标记成交生成订单 → 录一笔收款
- [ ] 回「对客报价」步骤点「重新生成」→ **应被拒**，错误信息含具体报价号+订单号，**且订单/收款原封不动**
- [ ] 另建一条只有草稿报价、无订单的测试商机 → 重新生成 → **应弹确认框列出将被覆盖的单号** → 确认后正常覆盖
- [ ] 删除测试商机 + 其订单/收款（05 本身就是级联链的一环，建了记得删干净）

### 11 应收看板 + 标记已收款
- [ ] 给测试客户开一张发票（订单详情发票 Tab → 开发票），到期日设为**过去**（若 UI 不让设过去日期，用今天+短账期后等一天，或让 CTO 用脚本造一条 issued≥2026-08-10、due<今天的测试发票）
- [ ] 工作台「发票应收（按到期）」卡 → **该发票应出现在「已逾期」档**，金额/天数/客户正确
- [ ] 看板底部应显示「本看板统计 2026-08-XX 起开具的发票」（起始日说明）
- [ ] 在订单详情发票 Tab 点「标记已收款」→ **该发票应从看板消失**
- [ ] 点「撤销收款」→ **应重新出现在看板**
- [ ] 核对看板「应收总额/其中逾期」与逐条相加一致
- [ ] 确认另一张卡标题是「订单收款进度（未收满）」（两卡口径已区分）
- [ ] 删除测试发票/订单
- ⚠ **预期**：上线初期看板基本为空——起始日 2026-08-10 把历史 21 张挡在外，**这是对的，不是「没逾期」**

## 3 · 只能冒烟、UI 无法触发实际修复路径的（诚实标注）

### 10 seed 硬编码密码
生产库**已有 admin 行**（老板确认），所以 seed 的建账号分支**在生产上根本不会进入**——
「随机密码/后门失效」只在**全新空库**才观测得到（本地已验）。生产这边只能验「没搞坏」：
- [ ] 部署后首页无 500（第 0 节已含）
- [ ] 系统设置→账户管理：**用户列表一个没变、没有冒出新 admin**
- [ ] 系统设置：`bank_*` 现有值**没被清空**（改的是默认值，`INSERT OR IGNORE` 不动现网值）

### 08 补录路径写快照
🔴 **线上前端没有「录入历史订单」入口**（`OrdersPage` 未被 import，已摇树剔除）——
**这条路径 UI 根本点不到，无法通过界面验证**。可达的等价验证是走**正常开票**（同一个
`_buildInvoiceSnapshot` 函数）：
- [ ] 给测试订单正常开一张发票 → 查库该发票 `invoice_entity_name`/`invoice_bank_*` **有值不空**
- [ ] 打开该发票打印页，抬头银行显示正常
- 补录路径本身的参数绑定：本地已直接驱动 `importHistoricalOrder` 实跑通过（08 单结论），生产无 UI 可复现

### 22 currency 三元兜底
前端表单**总会带 currency 字段**，所以 UI 正常操作**触发不到**这个 bug（它只在不带该字段的调用方才崩）。
生产验收 = 冒烟：
- [ ] 正常建/存商品（后台 + 供应商门户各一次）不报错——确认修复没引入回归
- 实际的「不带 currency → 落 IDR 不再 500」本地已验，生产无 UI 路径可复现

## 4 · 收尾

- [ ] 所有测试数据（客户/供应商/商品/商机/订单/发票/收款）全部删除
- [ ] 把每单实际看到的**提示文字 / 查库结果**回填进对应单子的「线上验证记录」，勾上生产 checkbox
- [ ] 11 号单顺带记一下：生产上 `receivables.summary` 的实际数字（老板最常问的「应收总额/逾期」）

---

**说明**：03 号单（import 清理）阻塞在老板澄清，不在本清单。列表逾期标记（11 交付③）
等 B 的 12/13 生产验收后单独做，也不在本清单。
